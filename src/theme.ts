import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#1e88e5' },
    success: { main: '#43a047' },
    warning: { main: '#fb8c00' },
    error: { main: '#e53935' },
    info: { main: '#8e24aa' },
  }
});
